/**
 * Tailwind CSS 4 is configured CSS-first — there is no `tailwind.config.js`.
 * Theme tokens live in the `@theme` block in `app/globals.css`.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
